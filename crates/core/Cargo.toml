[package]
name = "core"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "outline_core"

[features]
default = ["native"]
native = ["lopdf/default"]

[dependencies]
anyhow = "1.0"
lopdf = { version = "0.38.0", default-features = false }
serde = { version = "1", features = ["derive"] }
