{
  "name": "web-editor",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "esbuild vditor_entry.js --bundle --format=iife --platform=browser --metafile=meta.json --minify --outfile=../src/main/resources/web/vditor.bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "vditor": "^3.10.7"
  },
  "devDependencies": {
    "esbuild": "^0.27.0"
  }
}
