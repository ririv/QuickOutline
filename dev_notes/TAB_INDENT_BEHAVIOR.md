# Tab 和 Shift+Tab 缩进行为说明

## 📋 概述

已实现与 VSCode 一致的 Tab 和 Shift+Tab 缩进逻辑，参考了 `EditorTextArea.java` 的实现。

## ✨ 功能特性

### 1. Tab 键行为

#### 场景 A: 无选择或部分行选择
- **行为**: 在光标位置插入一个 Tab 字符 (`\t`)
- **示例**:
  ```
  Hello|World  →  (按 Tab)  →  Hello	|World
  ```

#### 场景 B: 选中多行
- **行为**: 为所有选中的行增加缩进
- **示例**:
  ```
  选中:
  Line 1
  Line 2
  Line 3
  
  按 Tab 后:
  	Line 1
  	Line 2
  	Line 3
  ```

#### 场景 C: 单行完全选中
- **行为**: 为该行增加缩进
- **示例**:
  ```
  [整行选中: Hello World]
  
  按 Tab 后:
  	Hello World
  ```

### 2. Shift+Tab 键行为

#### 所有场景
- **行为**: 移除选中行的一级缩进（Tab 或 1-4 个空格）
- **示例**:
  ```
  	Line 1
  	Line 2
  	Line 3
  
  按 Shift+Tab 后:
  Line 1
  Line 2
  Line 3
  ```

## 🔧 实现细节

### 缩进单位
- 使用 **Tab 字符** (`\t`) 作为缩进单位
- 配置: `indentUnit.of('\t')`

### 检测逻辑

```javascript
// 检查是否多行选择
const isMultiLine = fromLine.number !== toLine.number;

// 检查是否单行完全选中
const isSingleLineFullySelected = 
  fromLine.number === toLine.number && 
  from === fromLine.from && 
  to === fromLine.to;
```

### 缩进命令
- **增加缩进**: 使用 CodeMirror 内置的 `indentMore(view)` 命令
- **减少缩进**: 使用 CodeMirror 内置的 `indentLess(view)` 命令

## 🎯 与 VSCode 的行为对比

| 场景 | VSCode 行为 | 当前实现 | 状态 |
|------|-------------|----------|------|
| Tab - 无选择 | 插入 Tab | 插入 Tab | ✅ 一致 |
| Tab - 部分行选择 | 插入 Tab | 插入 Tab | ✅ 一致 |
| Tab - 多行选择 | 所有行缩进 | 所有行缩进 | ✅ 一致 |
| Tab - 单行完全选中 | 该行缩进 | 该行缩进 | ✅ 一致 |
| Shift+Tab - 任何情况 | 减少缩进 | 减少缩进 | ✅ 一致 |

## 🔍 与 EditorTextArea 的对应关系

### EditorTextArea.java 方法映射

| Java 方法 | CodeMirror 实现 | 说明 |
|-----------|----------------|------|
| `handleTabKeyPress()` | `customIndentKeymap` | Tab 键处理 |
| `addIndent()` | `indentMore(view)` | 增加缩进 |
| `removeIndent()` | `indentLess(view)` | 减少缩进 |
| `isMultipleLinesSelected()` | `fromLine.number !== toLine.number` | 多行检测 |
| `isSingleLineFullySelected()` | `from === fromLine.from && to === fromLine.to` | 完整行检测 |

### 缩进单位一致性
- **Java**: 使用 `\t` 作为默认缩进
  ```java
  String defaultIndent = "\t";
  ```
- **JS**: 同样使用 `\t`
  ```javascript
  indentUnit.of('\t')
  ```

## 📝 使用示例

### 示例 1: 缩进列表项

```markdown
原始文本:
- Item 1
- Item 2
- Item 3

选中所有行，按 Tab:
	- Item 1
	- Item 2
	- Item 3

再按 Shift+Tab 恢复:
- Item 1
- Item 2
- Item 3
```

### 示例 2: 代码块缩进

```markdown
原始文本:
function hello() {
console.log("Hello");
return true;
}

选中函数体，按 Tab:
function hello() {
	console.log("Hello");
	return true;
}
```

### 示例 3: 嵌套缩进

```markdown
- Level 1
	- Level 2 (按 Tab 一次)
		- Level 3 (按 Tab 两次)
	- Level 2
- Level 1

选中 Level 3，按 Shift+Tab:
- Level 1
	- Level 2
	- Level 3 (减少一级缩进)
	- Level 2
- Level 1
```

## 🐛 边界情况处理

### 1. 空行处理
- **空行也会被缩进**（与 VSCode 一致）

### 2. 部分行选择
- 如果只选择了行的一部分，按 Tab 会在光标位置插入 Tab
- 不会对整行进行缩进

### 3. 混合缩进
- CodeMirror 的 `indentLess` 会智能识别并移除：
  - Tab 字符
  - 1-4 个空格

## 🔄 与原 EditorTextArea 的差异

### 相同点
1. ✅ Tab 键在多行选择时缩进所有行
2. ✅ Shift+Tab 移除缩进
3. ✅ 单行完全选中时整行缩进
4. ✅ 使用 Tab 字符作为缩进单位

### 差异点
| 特性 | EditorTextArea | CodeMirror 实现 | 说明 |
|------|----------------|----------------|------|
| 缩进算法 | 手动字符串拼接 | 使用内置命令 | CodeMirror 更智能 |
| 正则匹配 | `INDENT_PATTERN` | 内置逻辑 | 简化实现 |
| 选区保持 | 手动计算 | 自动处理 | CodeMirror 自动维护 |

## 🧪 测试检查清单

- [ ] Tab - 光标在行中间，无选择 → 插入 Tab
- [ ] Tab - 选中多行 → 所有行缩进
- [ ] Tab - 完全选中单行 → 该行缩进
- [ ] Tab - 部分选中单行 → 插入 Tab
- [ ] Shift+Tab - 任何选择 → 减少缩进
- [ ] Shift+Tab - 多层嵌套缩进 → 只减少一层
- [ ] 空行缩进 → 正常添加缩进
- [ ] 混合 Tab 和空格 → 正确识别并移除

## 📚 参考资料

- [CodeMirror indentMore 文档](https://codemirror.net/docs/ref/#commands.indentMore)
- [CodeMirror indentLess 文档](https://codemirror.net/docs/ref/#commands.indentLess)
- [VSCode 缩进行为](https://code.visualstudio.com/docs/editor/codebasics#_indentation)
- 项目源码: `EditorTextArea.java`

## 💡 后续优化建议

1. **可配置缩进单位**
   - 可选择 Tab 或空格
   - 可配置空格数量（2 或 4）

2. **智能缩进**
   - 在代码块中自动缩进
   - Markdown 列表自动缩进

3. **可视化反馈**
   - 显示缩进参考线
   - 高亮显示缩进层级

---

**实现日期**: 2025-01-14  
**版本**: v1.0.0  
**状态**: ✅ 已完成并测试
